schemaVersion: 2.1.0
metadata:
  name: terrakube-platform
components:

  - name: tools
    container:
      image: quay.io/devfile/universal-developer-image:ubi8-c63ad83
      memoryLimit: 4Gi
      mountSources: true
      endpoints:
        - exposure: none
          name: debug
          protocol: tcp
          targetPort: 5005
        - exposure: public
          name: 8080-tcp
          protocol: http
          targetPort: 8080
      volumeMounts:
        - name: m2
          path: /home/user/.m2

  - name: maven
    container:
      image: docker.io/maven:3.9.0-eclipse-temurin-17
      memoryLimit: 2Gi
      mountSources: true
      volumeMounts:
        - name: m2
          path: /home/user/.m2
      command: [ 'tail' ]
      args: [ '-f', '/dev/null' ]

  - name: node
    container:
      image: docker.io/node:lts-bullseye-slim
      memoryLimit: 3Gi
      command: [ 'tail' ]
      args: [ '-f', '/dev/null' ]

  - name: minio
    container:
      image: docker.io/bitnami/minio:2022
      memoryLimit: 1Gi
      endpoints:
        - exposure: public
          name: minio9000
          protocol: http
          targetPort: 9000
        - exposure: public
          name: minio9001
          protocol: http
          targetPort: 9001
      command: ['tail']
      args: ['-f', '/dev/null']

  - name: m2
    volume:
      size: 1G

commands:
  - id: maven-build
    exec:
      component: maven
      workingDir: ${PROJECT_SOURCE}
      commandLine: |
        bash /projects/terrakube/scripts/che/buildApi.sh
      group:
        kind: build
        isDefault: false
